import { SimplePage } from './SimplePage';

export function NotFoundPage() {
  return <SimplePage eyebrow="Error 404 / route not found" title="Let us get you back to the right system." copy="The page may have moved, or the product route may need a fresh search. Browse the catalogue or send the team a project brief." href="/quote-request/" linkLabel="Start a quote brief" />;
}
